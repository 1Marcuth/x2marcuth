import { FC } from "react"

type Props = {}

const AppFooterLogo: FC<Props> = ({  }) => {
    return (
        <span className="text-white text-center font-bold text-xl">X2Marcuth</span>
    )
}

export default AppFooterLogo